# LogiFlow Admin

Standalone frontend for the LogiFlow administration dashboard.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

Application source lives in `src/`, with shared layout and UI primitives under
`src/components/` and route-level screens under `src/pages/`.
